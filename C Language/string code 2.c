#include <stdio.h>
int main()
{
    char str1[10];
    printf("enter the string ");
   // gets(str1);
    //fgets(str1,sizeof(str1),stdin);
    
    printf ("you enter == ");
    puts(str1);
    return 0;
}